DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_password_here
PORT=3000

# Frontend page that shows the set-password form
FRONTEND_URL=http://localhost:5173
INVITE_EXPIRY_HOURS=48

# Optional SMTP settings — if unset, invite links are logged/returned for development
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Bookstore <noreply@example.com>
